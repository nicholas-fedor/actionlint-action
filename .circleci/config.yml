version: 2.1

executors:
  default:
    docker:
      - image: cimg/node:lts@sha256:0a67b6fa151ceac233d469a5915ca75a4a1c9e3854502ed7cc236f18c3a4f5c3
    working_directory: ~/project

commands:
  setup-and-install:
    steps:
      - checkout
      - run:
          name: Install Bun
          command: |
            curl -fsSL https://bun.sh/install | bash
            echo 'export PATH="$HOME/.bun/bin:$PATH"' >> $BASH_ENV
      - run:
          name: Install Dependencies
          command: |
            export PATH="$HOME/.bun/bin:$PATH"
            bun install

  verify-dependencies:
    steps:
      - run:
          name: Audit Dependencies
          command: |
            export PATH="$HOME/.bun/bin:$PATH"
            bun audit

  build-dist:
    steps:
      - run:
          name: Build Distribution Bundle
          command: |
            export PATH="$HOME/.bun/bin:$PATH"
            bun run build

jobs:
  test:
    executor: default
    steps:
      - setup-and-install
      - verify-dependencies
      - run:
          name: Run Tests with Coverage
          command: |
            export PATH="$HOME/.bun/bin:$PATH"
            bun test --coverage
      - run:
          name: Upload Coverage to Codecov
          command: |
            if [ -n "$CODECOV_TOKEN" ]; then
              curl -Os https://uploader.codecov.io/latest/linux/codecov
              chmod +x codecov
              ./codecov -t "$CODECOV_TOKEN" --dir ~/project/coverage
            else
              echo "CODECOV_TOKEN not set, skipping upload"
            fi
      - persist_to_workspace:
          root: ~/project
          paths:
            - coverage

  test-pr:
    executor: default
    steps:
      - setup-and-install
      - verify-dependencies
      - run:
          name: Run Tests
          command: |
            export PATH="$HOME/.bun/bin:$PATH"
            bun test
      - build-dist

workflows:
  ci:
    jobs:
      - test:
          filters:
            branches:
              only: main
            tags:
              only: /.*/
      - test-pr:
          filters:
            branches:
              ignore: main
